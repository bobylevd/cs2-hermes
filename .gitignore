# Secrets / runtime config
.env

# Runtime persistence state (the agent writes here); keep the dir, not contents
custom_files/*
!custom_files/.gitkeep

# Hermes runtime state (SOUL.md, skills, memory) — host-mounted, machine-local
hermes-data/

# JetBrains IDE
.idea/
