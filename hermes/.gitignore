# This folder IS the live HERMES_HOME (bind-mounted). Hermes writes runtime state
# here — memory, sessions, logs, node/, caches, .env. Track only curated files.
*
!.gitignore
!SOUL.md
!USER.md
!AGENTS.md
!config.yaml

